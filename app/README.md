# app

React + TypeScript frontend (Vite). Part of the root pnpm workspace — run `pnpm install` from the repo root.

```bash
cp .env.example .env
pnpm dev              # watch mode, http://localhost:5173
```
